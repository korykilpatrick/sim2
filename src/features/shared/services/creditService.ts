/**
 * Re-export the unified credit service
 * 
 * This file provides backwards compatibility for code that imports
 * the credit service from the shared location.
 */

export { creditService } from '@/features/credits'